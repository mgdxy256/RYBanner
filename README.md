# RYBanner
轮播图

### 使用方法
```
var ryBanner =document.getElementById("ry-banner");
RYBanner()
    .register(ryBanner)                         // 注册控件
    .setInterval(1000).                         // 设置自动播放间隔
    setImageList([{url:'xxx',img:'xxx'},...];   // 设置图片
```
